//@version=4
// set05 corpus entry
// source_repo: pinescript-agents
// source_path: pinescript-agents/docs/docs/v4/language/functions-and-annotations.md::functions-and-annotations.md#block5
// content_hash: a4bd7c8f19fc1ce1
// collected: 2026-07-27
study("My Script")
va = 0.0
for i = 1 to 2 by 1
    if (i + bar_index) % 2 == 0
        va := lowest(bar_index, 10)  // same context on each call
plot(va)         Previous       Operators      Next   Expressions, declarations and statements              On this pageOverviewExecution of Pine functions and historical context inside function blocksWhy this behavior?Exceptions
