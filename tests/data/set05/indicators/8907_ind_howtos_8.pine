//@version=3
// set05 corpus entry
// source_repo: pinescript-agents
// source_path: pinescript-agents/docs/docs/v3/appendix/howtos.md::howtos.md#block10
// content_hash: ac4071f50b405c0e
// collected: 2026-07-27
study("My Script")
series = close >= open ? close : na
vw = valuewhen(not na(series), series, 0)
plot(series, style=linebr, color=red) // series has na values 
plot(vw) // all na values are replaced with the last non-empty value         Previous       Pine version 3 migration guide      Next   Pine compilation errors              On this pageGet real OHLC price on a Heikin Ashi chartPlot arrows on the chartPlot a dynamic horizontal linePlot vertical line on conditionAccess previous valueGet a 5-day highCount bars in a datasetenumerate bars in a dayFind the highest and lowest values for the entire datasetQuery the last non-na value
