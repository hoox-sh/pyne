//@version=4
// set05 corpus entry
// source_repo: pinescript-agents
// source_path: pinescript-agents/docs/docs/v4/debugging.md::debugging.md#block11
// content_hash: 4254b23317a6bc4d
// collected: 2026-07-27
study("Debugging from inside functions", "", true)
f_hlca() =>
    var float _avg = na
    _instantVal = avg(high, low, close, nz(_avg, close))
    _avg := sma(_instantVal, 20)
    // Return two values instead of one.
    [_avg, _instantVal]

[hlca, instantVal] = f_hlca()
plot(hlca, "hlca")
plot(instantVal, "instantVal", color.black)
