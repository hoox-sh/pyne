//@version=6
// set05 corpus entry
// source_repo: pinescript-agents
// source_path: pinescript-agents/docs/docs/errors/RE10143.md::RE10143.md#block5
// content_hash: a594060362215520
// collected: 2026-07-27
indicator("Realtime error with drawings")

max_bars_back(time, 500)

if barstate.isrealtime
    line.new(bar_index[500], close, bar_index, close)         Previous       RE10139           On this pageThe requested historical offset (X) is beyond the historical buffer’s limit (Y)Potential fixesUse the max_bars_back() functionUse the max_bars_back parameter of the indicator() or strategy() functionUse the maximum value manually on history to force a proper buffer sizeMax bars back with Pine drawings
