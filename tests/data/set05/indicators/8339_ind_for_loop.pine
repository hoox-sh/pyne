//@version=4
// set05 corpus entry
// source_repo: SammyEnigma-pine-scripts
// source_path: SammyEnigma-pine-scripts/for-loop.pine
// content_hash: 18047ab9828a132d
// collected: 2026-07-27
study("For loop")
my_sma(price, length) =>
    sum = price
    for i = 1 to length-1
        sum := sum + price[i]
    sum / length
plot(my_sma(close,14))
