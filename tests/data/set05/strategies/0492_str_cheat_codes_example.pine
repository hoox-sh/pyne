//@version=5
// set05 corpus entry
// source_repo: hasnocool-tradingview-pine-scripts
// source_path: hasnocool-tradingview-pine-scripts/Cheat Code- Example 1; Short-Term; Follow the Trend.pine
// content_hash: fbb9acbb697dffab
// collected: 2026-07-27
//SUBSCRIBE TO CHEAT CODE!!!
//
//
//
//SUBSCRIBE TO CHEAT CODE!!!
strategy("Cheat Codes Example", overlay = true)
//RSI
rsi = ta.rsi(close, 14)
//ENTRY
LC1 = ta.crossover(ta.sma(close,21), ta.sma(close,100))
LC2 = ta.sma(close, 200) > close
if (LC1) and (LC2) 
    strategy.entry("BULL CALL", strategy.long)
    
SC1 = ta.sma(close, 200) < close
SC2 = ta.crossunder(rsi, 55)
if (SC1) and (SC2)
    strategy.entry("BEAR CALL", strategy.short)
    
    // BEST PAIRINGS:
    // BTC: 10M
    // MATIC: 10M
    // ETH: 10M
    // FIL: 10M
    // :) 
Expand (37 lines)
