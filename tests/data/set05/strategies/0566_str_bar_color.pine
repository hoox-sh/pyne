//@version=5
// set05 corpus entry
// source_repo: hasnocool-tradingview-pine-scripts
// source_path: hasnocool-tradingview-pine-scripts/Bar Color Gr38612.pine
// content_hash: 0e0d62d37dd8081d
// collected: 2026-07-27
strategy("Bar Color", overlay=true, process_orders_on_close = true)
 
long_condition  = close>open
short_condition = close<open
 
plotshape(long_condition,  color = color.green)
plotshape(short_condition, color = color.red)
 
if (long_condition)
    strategy.entry("Long", strategy.long)
    strategy.close("Short") 
if (short_condition)
    strategy.entry("Short", strategy.short)
    strategy.close("Long")
Expand (16 lines)
