{
  "name": "pyne-api-container",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types",
    "dev": "wrangler dev",
    "build:image": "docker build --platform linux/amd64 --target api -t pyne-api:cf ..",
    "deploy": "npm run build:image && wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.127.1"
  }
}
