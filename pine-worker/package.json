{
  "name": "pine-worker",
  "version": "1.0.0",
  "description": "TypeScript port of the pynescript evaluator (Plan 2 of pine-worker-strategy-events). Extra tool colocated in the main pynescript repo.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'add eslint or biome here'",
    "parity": "bun test test/parity --timeout 30000 || echo 'parity harness not yet wired (see plan)'"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
